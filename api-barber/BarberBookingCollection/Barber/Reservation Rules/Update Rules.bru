meta {
  name: Update Rules
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/barber/reservation-rules
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "maxAdvanceBookingDays": 30
  }
}
